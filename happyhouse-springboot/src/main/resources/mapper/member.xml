<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.MemberMapper">

	<select id="login" resultType="MemberDto" parameterType="MemberDto">
		SELECT userid, username 
		FROM happyhouse_member
		WHERE userid = #{userid} and userpwd = #{userpwd}
	</select>
	<insert id="join" parameterType="MemberDto">
		INSERT INTO happyhouse_member (userid, username, userpwd, email, admin, joindate)
		values(#{userid}, #{username}, #{userpwd}, #{email}, 0, now())
	</insert>
	<update id="memberupdate" parameterType="MemberDto">
		update happyhouse_member
		set username = #{username}, userpwd = #{userpwd}, email = #{email}
		where userid = #{userid}
	</update>
	
	<delete id="memberdelete" parameterType="String">
		delete from happyhouse_member
		where userid = #{userid}
	</delete>
	<select id="memberdetail" parameterType="String">
		select *
		from happyhouse_member
		where userid = #{userid}
	</select>
</mapper>